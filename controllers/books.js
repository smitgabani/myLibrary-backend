const pdb = require('../config/database');
const Book = require('../models/Book');

module.exports = {
    async getAllBooks(req, res, next) {},
    async getBook(req, res, next) {},
    async postNewBooks(req, res, next) {}
}